<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">
  </head>

  <body>
    <header class="header">
      <div class="header_logos">
        <%# !=not演算子を使用しindex.html.erb以外の場合は戻るボタンを表示するif文 %>
        <% if !current_page?(root_path) %>
          <%=  link_to :back do%>
            <%= image_tag 'back.svg', class: 'header_back' %>
          <% end %>
        <% end %>
        <p class="header_pageTitle">BlogApp</p>
      </div>
      
      <div class="dropdown">
        <%= image_tag 'default-avatar.png', class: 'header_avatar dropbtn' %>
        <div class="dropdown-content">
          <a href="#">プロフィール</a>
          <a href="#">ログアウト</a>
        </div>
      </div>
    </header>
    <% if flash.present? %>
      <div class="flash">
        <% flash.each do |key, value| %>
          <div class="<%= key %>"><%= value %></div>
        <% end %>
      </div>
    <% end %>

    <%# application.html.erbがブラウザ上に表示される %>
    <%# application.html.erbの yield の中にindex,about.html.erbがある %>
    <%# ∴ﾍｯﾀｰやﾌｯﾀｰなどの全ページ共通項目はapplication.html.erbに記載する %>
    <%= yield %>
  </body>
</html>
